__pycache__/
# bare bytecode outside __pycache__ dirs — the stray-.pyc incident class of
# the 2026-08-31 scrub (ticket 27 H5); never tracked, anywhere in the tree
*.pyc
.pytest_cache/
.ruff_cache/
# runtime-generated by the add-on (ticket 17), never shipped in the payload
payload/anki_theme/web/
# tier-2/3 gate outputs: screenshots, probe reports, scratch bases (ticket 22)
tests/gate/artifacts/
# vendored Pillow for the gate's PIL sampling (python-pillow, cp314 wheel);
# bootstrap: python -m pip install --target tests/gate/vendor pillow
tests/gate/vendor/
# internal dev tracker (decision log) — kept local, never published
.scratch/
.video_agent
