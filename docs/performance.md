# Performance record

Append-only log. Every entry is dated, tied to a commit, and states the method and
machine; regressions get investigated, never erased.

## Standing metrics

- **switch-to-reapply** — time from `omarchy-theme-set` completion to Anki visually
  recolored, same-polarity switch (dark→dark palette change, the case Anki ignores
  natively). Measured from add-on-side timestamps in the log, cross-checked once
  against a screen recording frame diff.
- **add-on startup cost** — ms added to Anki profile load with the add-on enabled
  vs. disabled (mean of ≥5 runs).

## Log

Provenance note (appended 2026-09-01, ticket 27 H10): several rows below
carry SHAs from before the 2026-08-31 history scrub and metadata rewrite
(privacy pass) — those commits no longer resolve in this repository, and the
measurements stand as recorded. `ankiya` in older method notes is the add-on
folder's name before the same-day rename to `anki_theme` (same code).

| Date | Commit | Metric | Value | Method | Notes |
| ---- | ------ | ------ | ----- | ------ | ----- |
| 2026-08-29 | 3a31695 | dev-loop: watcher/IPC plugin reload | < 1 s (same-second) | journal timestamps of `Local plugin changed` → service mount proof-file rewrite | Reload re-instantiates but runs **stale** compiled QML (upstream basecamp/omarchy#6981) — not a valid apply path for code changes |
| 2026-08-29 | 3a31695 | dev-loop: `omarchy restart shell` (applies QML code) | ~0.4 s command return, service remounted ~2 s later | `time omarchy restart shell` + proof-file `mountedAt` delta; Ryzen 5 7500F, Arch, quickshell 0.3.1-1, Omarchy 4.0 line | The mandatory step per QML code change (ticket 05) |
| 2026-08-29 | 1264eb4 | ticket 06 prototype runs (mocha + latte) | not measured — standing metrics don't apply | Throwaway spike on the live install (Ryzen 5 7500F, Arch): no live-switch applier exists yet to time and nothing shipped on main | Recorded to close a review gap: first switch-to-reapply and startup-cost numbers land with the ticket-07/09 applier |
| 2026-08-29 | e3118de | switch-to-reapply (spike, all 3 legs incl. same-polarity) | recolor completes **265–283 ms before `omarchy theme set` returns** (i.e. ≤ 0 ms after completion); in-app apply 12.6–14.8 ms; end-to-end from command invocation 426–433 ms | Add-on-side timestamps joined to driver timings (`legs.jsonl` × `applied.log`), recolor pixel-verified hard (menubar, web canvas, open Add window) in before/after shots; ticket 09 spike, branch `prototype/09-live-switch`; Ryzen 5 7500F, Arch, Anki 26.08.1, Omarchy 4.0 line | First real numbers; screen-recording frame-diff cross-check still pending (single method so far) |
| 2026-08-29 | e3118de | add-on startup cost (spike, preliminary) | 4.3 ms startup palette apply (single run) | Time from `profile_did_open` to applied marker, spike add-on; same machine | NOT the standing metric yet — that needs the ≥5-run profile-load delta with the real add-on |
| 2026-08-30 | 59db5c0 | switch-to-reapply (ticket 17 real runtime) | in-app apply **5.3 ms** (light→light Flexoki Light) / **6.2 ms** (dark→dark Gruvbox, Ethereal); end-to-end from invocation 436–463 ms; recolor completes ~2.9 s **before** `omarchy theme set` returns | `applied.jsonl` records (apply_ms, applied_at) joined to driver timings from `tests/smoke_live_switch.py` runs on a scratch Anki base; legs asserted (51 vars both slots, 3 views restyled, engine profile scripted, delivered CSS carries target `--canvas`); Ryzen 5 7500F, Arch, Anki 26.08.1, Omarchy 4.0 line | Real add-on runtime, both polarities incl. the critical same-polarity dark→dark; pixel-level rendering proof lands with tier 2 (ticket 22) per docs/verification.md |
| 2026-08-30 | 59db5c0 | add-on startup cost (ticket 17, preliminary) | startup palette apply 3.7–4.3 ms (3 scratch runs) | `applied.jsonl` startup records, same machine | Still not the standing metric — the ≥5-run profile-load delta with/without the add-on lands with ticket 19's bootloader |
| 2026-08-30 | c5bf20d | switch-to-reapply (ticket 18, clamp wired into `_map`) | in-app apply **6.7 ms** (light→light Flexoki Light) / **6.9 ms** (dark→dark Gruvbox); clamp adds **~11 µs** per apply on stock palettes, **~2.3 ms** per pathological nudge | Same smoke method as the ticket-17 rows (scratch base, `applied.jsonl` apply_ms; `clamped == 0` asserted on the stock switch); clamp isolation timed directly: `map_with_clamp` on system python, 2000-call mean stock / 100-call mean pathological; same machine | Delta vs 5.3–6.2 ms is apply jitter, not clamp cost (isolated stock cost is µs-scale); the ~2.3 ms nudge scan fires only on hostile user palettes, still inside the 50 ms budget |
| 2026-08-30 | c5bf20d | clamp scan optimization (code-review pass 2) | pathological `map_with_clamp` **5.15 → 2.30 ms**/call (fill luminances hoisted out of the 1024-step scan) | Direct timing before/after the hoist, same method as above | Review finding; results provably identical (P4/P5 analytic max-min windows unchanged) |
| 2026-08-30 | c69a8e6 | add-on startup cost (ticket 19 — **the standing metric**) | **18.5 ms** mean added to launch→profile-open (per-run deltas 14–29 ms across 5 pairs) | `tests/smoke_sync_bootloader.py` phase C on a scratch base: a `zz_probe` add-on (loads after `ankiya`, so its `profile_did_open` hook fires after Ankiya's start) timestamps profile open in-process; 6 launches with the add-on enabled vs 6 disabled via meta.json `disabled`, first of each dropped as Qt-cache warmup, mean of the remaining 5 pairs; Ryzen 5 7500F, Arch, Anki 26.08.1, Omarchy 4.0 line | First A/B measurement of the whole add-on (imports + bootloader sync + hook wiring + startup apply) — the 3.7–4.3 ms preliminary rows counted the apply alone |
| 2026-08-30 | c69a8e6 | sync routine costs (ticket 19) | boot-time current-check **0.30 ms** (hash + stamp read); `tree_hash` 0.33 ms (16-file bundle); install 0.77 ms; swap 0.88 ms | Direct timing of the pure routine on system python: 200-call mean hash, 50-call mean current-check, 20-call mean install, single swap, tmp-disk trees; same machine | The current-check runs at every Anki start (inside the 18.5 ms above); swap/install run only on drift |
| 2026-08-30 | dbc63d0 | service-start decision (ticket 20, gate subprocess) | **50.7 ms** mean per service start (min 48.9 / max 52.7, 20 runs) | End-to-end wall time of `/usr/bin/python -B service/gate.py <anki2> <state>` including the `omarchy-version` spawn (decision path `ask_consent`, read-only, real dirs); Ryzen 5 7500F, Arch, Anki 26.08.1, Omarchy 4.0.1-1 | One-shot at each service start (shell login, `omarchy restart shell`, plugin enable); interpreter startup dominates. Standing metrics untouched by ticket 20 |
| 2026-08-30 | dbc63d0 | post-consent service start (ticket 20, sync subprocess) | **30.1 ms** mean (min 28.6 / max 31.8, 20 runs) | `/usr/bin/python -B payload/ankiya/sync.py …` end-to-end against a scratch installed-and-current tree (`status: current`); same machine | Runs once per service start after consent; the routine inside is ticket 19's 0.30 ms current-check — the rest is interpreter startup |
| 2026-08-30 | dbc63d0 | consent click-through (ticket 20, grant helper) | **55.5 ms** mean (min 54.2 / max 56.4, 5 runs) | `/usr/bin/python service/grant.py <scratch Anki2> <scratch state>`: consent write + Sync install subprocess, end to end; same machine | One-shot per toast click; a re-click on recorded consent costs the 30 ms sync-current leg alone |
| 2026-08-30 | 11bdad2 | drift startup check (ticket 21) | **0.024 ms** clean-start (the every-start path); 0.065 ms surfaced-retract second start | Direct timing of `run_check`'s composition on system python against the real `aqt.colors`: `diff_inventory(gather_inventory(ak), load_snapshot(snapshot))`, 200-call mean; surfaced path single-shot including the real `drift_seen.json` read in a tmp state dir; Ryzen 5 7500F, Arch, Anki 26.08.1 | Runs once per process inside `runtime.start()` — noise against the 18.5 ms standing startup metric (untouched; the ticket-19 boot smoke re-ran PASS with the check wired in, 8.5 ms that session's jitter); the surfaced end-to-end path (tooltip render) is tier-3 drift-smoke territory (ticket 23) |
| 2026-08-30 | b617cdd+ | in-app apply cost vs restyled-view count (ticket 23, full-matrix sessions) | **12.4 ms @ 4 views** (first switch) → 62–104 ms @ 5–6 → plateau **@ 7 views, session-variable: 150–176 ms (run 1) / 231–262 ms (run 2)**; ~25–40 ms marginal per restyled webview; polarity flips add ~60–115 ms on top (aqt `_apply_style()` re-polish); swap→applied up to 0.34 s at run 1's plateau | Two full `pytest -m gate_full` matrix sessions on the same tree (same Anki process, 22 stock switches; `switch-<theme>.json` records' `apply_ms`/`views`/`polarity_flip`), cross-checked against a 3-leg sequence run (12.4 → 68.4 → 103.4 ms at 4 → 5 → 6 views); Ryzen 5 7500F, Arch, Anki 26.08.1 | **Regression-shaped, investigation deferred** (decision 2026-08-30): views accumulate as the session opens dialogs (4 → 7), each restyle `view.eval()` costing renderer IPC; short-session cost stays ~7–18 ms (all earlier rows). In-tree gate budgets (apply 200/250 ms, swap 0.450/0.500 s) cover run 1 but **not** run 2's plateau — next session raises them (≈ 400/450 ms, swap ≈ 0.650/0.700 s) or scales them per view; the standing-metric populations above are unaffected (short sessions, ≤ 4 views) |
| 2026-08-30 | 4dce4e0+ | **timing DE-GATED from the gate** (user directive: "Do not worry about performance right now — the perf could be recorded but we would address perf when everything passes in a different ticket") + tier-3 green-run session row | all 33 matrix switches: apply **21–1043 ms (median 769)**, swap→applied 199–1228 ms; 5-switch session mean apply **1031.9 ms** / swap→applied **1214.3 ms**; startup-cost standing metric re-measured **17.4 ms** mean (5 pairs, deltas 1.7–29.8) | The first fully green `pytest -m gate_full` (44/44, 617 s, artifacts `tests/gate/artifacts/20260830-223107/`, `perf-switch-records.jsonl` + `perf-switch-session.json` + `perf-startup-cost.json` + `perf-frame-diff.json`); Ryzen 5 7500F, Arch, Anki 26.08.1. **Screen-recording frame-diff cross-check landed** (the 2026-08-29 spike row's pending leg): 60 fps, recolor visible **1.73 s before `omarchy theme set` returned**, flip-interval video vs records **Δ −0.80 s** — applied_at is stamped after all restyle legs while the screen flips at the main webview's mid-loop repaint | This session ran immediately after a prior full run (back-to-back load; `omarchy theme set` itself took ~3 s, reapply-vs-set-done ≈ −1.8 s) — absolute costs are load-shaped, recorded not chased. The prior row's "next session raises the budgets" never happened: asserts became record-only instead; re-arming them with recalibrated values (or per-view scaling) is ticket 25's first act |
| 2026-08-31 | 3967902 | ticket 24 pre-flip confirmation: full gate on the **exact submission commit** | 34 matrix legs: apply **4–983 ms (median 729)**; 5-switch session mean apply **977.7 ms** / swap→applied **1157.5 ms** (reapply ≈ **1.87 s before** `omarchy theme set` returned); startup-cost standing metric **10.5 ms** mean (5 pairs, deltas 7.2–14.7) | Second fully green `pytest -m gate_full` (44/44, 610.75 s, artifacts `tests/gate/artifacts/20260831-075553/` — same four perf files; gallery built from it); tier 1 green on the same tree (209 passed); Ryzen 5 7500F, Arch, Anki 26.08.1 | Costs match the 2026-08-30 green run's load shape (accumulated-view plateau, record-only per the standing de-gating); startup cost 17.4 → 10.5 ms on this session's pairs. Ticket 25 still owns recalibrated re-arming |
| 2026-08-31 | 54bb14e+ | ticket 26 post-fix full gate (review-toolbar background re-copy wired) | 33 matrix legs: apply **14.5–1054 ms (median 780)**; 5-switch session mean apply **1039.2 ms** / swap→applied **1222.1 ms** (reapply ≈ **1.81 s before** `omarchy theme set` returned); startup-cost standing metric **15.9 ms** mean (5 pairs, deltas 0.1–21.5) | Third fully green `pytest -m gate_full` (**44/44**, 611.9 s, artifacts `tests/gate/artifacts/20260831-085016/` — same four perf files; gallery rebuilt, 27 themes); tier 1 **209 passed** + tier 2 **7 passed** (incl. the new mid-review polarity-flip leg, red/green-proven) on the same tree; Ryzen 5 7500F, Arch, Anki 26.08.1 | First run on the ticket-26 fixed tree. The new leg (clear stale inline background + one deferred re-copy per flip) fires only in review state — no delta vs the prior green runs' load shape (accumulated-view plateau, record-only per the standing de-gating). Ticket 25 still owns recalibrated re-arming |
| 2026-08-31 | ba6dc06 | ticket 24 rename-session full gate (`ankiya`→`anki_theme`, display-name reversal) | 34 matrix legs: apply **5–1066 ms (median 748)**; 5-switch session mean apply **1031.3 ms** / swap→applied **1213.7 ms**; startup-cost standing metric **18.9 ms** mean (5 pairs, deltas 9.4–30.9) | Fourth fully green `pytest -m gate_full` (44/44, 611.40 s, artifacts `tests/gate/artifacts/20260831-131037/`; gallery + `preview.png`/README pairs recomposed from it); tier 1 209 + tier 2 7/7 green on the same tree; Ryzen 5 7500F, Arch, Anki 26.08.1 | Pure rename (imports/paths/strings), no logic change; costs in family with the other green runs — the morning session's 10.5 ms startup was the session outlier, not a regression signal; ticket 25 owns recalibration |
| 2026-08-31 | a58647a | ticket 27 pre-flip hardening full gate (H1–H4: PEP 758 parens pinned formatter-stable, meta.json symlink guards, verified crash recovery, atomic no-follow writes) | 34 matrix legs: apply **4.1–1070.9 ms (median 821)**; 5-switch session mean apply **1053.5 ms** / swap→applied **1237.6 ms** (reapply ≈ **1.79 s before** `omarchy theme set` returned); startup-cost standing metric **9.2 ms** mean (5 pairs, deltas −0.2–14.4) | Fifth fully green `pytest -m gate_full` (**44/44**, 613.35 s, artifacts `tests/gate/artifacts/20260831-155217/` — same four perf files + frame-diff; gallery rebuilt from it); tier 1 **224 passed** (15 new hardening pins) + tier 2 **7/7** on the same tree; Ryzen 5 7500F, Arch, Anki 26.08.1 | Delivery-machinery hardening only — no theming/mapping code touched; costs in family with the four prior green runs (accumulated-view plateau, record-only per the standing de-gating); the H4 atomic writes (mkstemp 0600) sit on three file-write sites, none in the apply hot path. Ticket 25 still owns recalibrated re-arming |
| 2026-09-01 | 195b602 | **ticket 25 root-cause: the accumulated-view plateau decomposed** — per-leg apply timings landed + the leaked-DeckStats eval waste fixed | tier-2 family on the fixed tree: apply **11.9–16.7 ms @ 3–4 views** (all five legs), per-leg `qt_chrome 8.5–15.4 / web_css ≤1.5 / engine_scripts ≤0.3 / theme_did_change ≤1.8 / open_pages 0.2–2.2 ms`; two aqt-side causes isolated: (1) every DeckStats open strands a never-shown `LegacyStatsWebView` parented to mw (aqt/stats.py:156; `reject()` only nulls the reference) which the restyle walk eval'd forever — now skipped (hidden-mw-children skip, red/green tier-2 orphan-skip leg); (2) every Preferences open leaks a whole hidden dialog window (aqt's deleteLater GC notwithstanding) + the stats spares accumulate, so **aqt's own `_apply_style` re-polish (the qt_chrome leg) grows with the widget tree: 11.8 → 1029.3 ms across the 22-theme matrix** — the dominant plateau cost was never the eval loop; the earlier "~25–40 ms/restyled view" attribution was confounded (view count and widget count both tracked the theme index) | New gate `census` command (the walk's own expression, itemized) driven through two probe sessions (stats/prefs open-close cycles + genealogy while-open); retained-run log analysis (20260831-131037/155217: `restyled` 4→26 = one leaked spare per stats open, apply tracks it); per-leg breakdown = the runtime's `leg_ms` record field, asserted in-shape by tier 2; Ryzen 5 7500F, Arch, Anki 26.08.1 | Batched-eval question closed: no Qt batch API exists and dispatch isn't the cost (5 views ≈ 12 ms total; qt_chrome dominates at every count). The qt_chrome growth is aqt's pipeline recoloring a growing widget tree — not ours to skip (only live Qt-chrome path) or fix (aqt widget lifecycle); bounded by the gate instead (see next row). Upstream-report candidates against ankitects/anki (user's call, #5240 precedent) |
| 2026-09-01 | 195b602 | ticket 25 calibration: thresholds re-armed (ownership split), first green full gate with timing asserts live | matrix legs apply **13.1–1048.7 ms** (all of the tail = qt_chrome; owned legs max **web_css 0.4 / engine_scripts 0.3 / theme_did_change 2.1 / open_pages 21.5 ms**); 5-switch timing-session mean apply **1034.8 ms** / swap→applied **1217.4 ms** (aqt-dominated tail, attributed per-leg); frame-diff flip-interval delta **−0.018 s** (asserted ±0.35); startup-cost standing metric **14.4 ms** mean (5 pairs, deltas −0.4–23.6) | `pytest -m gate_full` **44/44 GREEN @ 611.85 s, artifacts `tests/gate/artifacts/20260901-134541/`** — asserts live: owned legs 25/25/50/100 ms, apply total 1500 ms runaway bound, swap→applied 2.0 s, startup 100 ms, frame interval ±0.35 s; tier 1 **249** + tier 2 **8/8** on the same tree (195b602); Ryzen 5 7500F, Arch, Anki 26.08.1 | A second full session the same hour was **invalidated externally, not a product signal**: a user window opened mid-run at the nord leg (restyled views 4→6 from exactly there; 14 deck-surface assert failures on foreign pixels; timings recorded and in-family) — discarded per user confirmation. The timing-session means match the pre-fix green runs (1031–1054 ms): the tail cost is the aqt-side qt_chrome growth, unaffected by the orphan skip — the short-session family (tier-2's 12–17 ms) is the user-realistic number |
| 2026-09-01 | 195b602 | startup-cost pair spread (ticket 25 thread 4 — analysis, no change) | per-pair deltas **9.4–30.9 ms** (mean 18.9, run 20260831-131037) vs **−0.2–14.4 ms** (mean 9.2, run 20260831-155217) — the spread tracks launch jitter, not add-on cost variance | Analysis of the two retained `perf-startup-cost.json` files (5-pair A/B on ~600 ms launches ±10 ms each; two noisy measurements per delta); session means differ by more than either run's internal spread → session-level launch conditions dominate | Conclusion: the ≥5-pair mean is the right method shape; the add-on's own costs are already measured in isolation (sync check 0.30 ms, drift 0.024 ms, startup apply ~4 ms). No further action |
| 2026-09-01 | 465a624 | sync current-check cost, H6 re-hash added (ticket 27: the CURRENT leg now verifies the installed tree's bytes, not just the stamp) | current-check **0.35 → 0.90 ms** (+0.55 ms; direct timing, 50-call mean, interleaved old/new, tmpfs, real 11-file/96 KB bundle minus dev artifacts); in-gate `perf-sync-costs.json` current_check_mean **1.08 ms** on the full-run scratch base | Same direct-timing method as the ticket-19 sync-routine row (2026-08-30), re-run old vs new on this machine by the H6 implementation subagent; gate number from the 44/44 session `20260901-161856`; Ryzen 5 7500F, Arch, Anki 26.08.1 | Runs once per Anki start and once per service start; the gate's startup standing metric the same session measured **5.6 ms mean** (deltas −0.7–10.7) — best-family, the +0.55 ms is inside launch noise |
| 2026-09-01 | 465a624 | **ticket 27 H5–H12 hardening full gate** (installed-tree re-verification on CURRENT; palette read capped 64 KiB; applied.jsonl rotation; grant canonical-dir guard; omarchy-version absolute-first; exec argv validation; gitignore/log/object-store hygiene) | 34 matrix legs: apply **4.2–1042.9 ms (median 779.6)**, owned legs max **web_css 0.4 / engine_scripts 0.3 / theme_did_change 1.8 / open_pages 19.6 ms** (aqt's qt_chrome carries the tail, max 1023.2 under the 1500 ms bound); 5-switch timing-session mean apply **1022.5 ms** / swap→applied **1204.4 ms** (reapply ≈ **1.81–1.85 s before** `omarchy theme set` returned); startup-cost standing metric **5.6 ms** mean (5 pairs, deltas −0.7–10.7); frame-diff flip-interval delta **+0.015 s** (±0.35 asserted) | `pytest -m gate_full` **44/44 GREEN @ 612.63 s, artifacts `tests/gate/artifacts/20260901-161856/`** (timing asserts live per ticket 25); tier 1 **277** (+28 hardening) + tier 2 **8/8** on the same tree; Ryzen 5 7500F, Arch, Anki 26.08.1 | Delivery/hardening only — no theming/mapping code touched; all populations in family with the five prior green runs (sessions 1031–1054 ms, startup 9.2–18.9). First full run the same hour was **41/44, discarded as harness+environment, not product**: the two tier-3 service legs tripped the new H9 canonical-dir and H12 resolver guards working as designed (harness adapted to scratch-HOME canonical state + pinned resolver candidates, re-proven green) and the frame-diff leg lost the second flip in the recording (green isolated ×1 and in this clean run) |
| 2026-09-03 | 256d6ab | **ticket 35 v0.1.2 full gate** (audit-cuts tree: tickets 30–34 + display-name rename + four-palette preview) | 34 matrix legs: apply **3.9–1067.0 ms (median 829.8)**, owned legs max **web_css 0.4 / engine_scripts 0.4 / theme_did_change 2.2 / open_pages 18.9 ms** (aqt's qt_chrome carries the tail, max 1047.1 under the 1500 ms bound); 5-switch timing-session mean apply **1038.7 ms** / swap→applied **1225.7 ms** (reapply ≈ **1.79–1.83 s before** `omarchy theme set` returned); startup-cost standing metric **9.5 ms** mean (5 pairs, deltas 0.6–13.1); frame-diff flip-interval delta **+0.013 s** (±0.35 asserted) | `pytest -m gate_full` **44/44 GREEN @ 609.62 s, artifacts `tests/gate/artifacts/20260903-144351/`** (gallery rebuilt, 27 themes); first session the same hour **43/44, discarded as environmental**: the frame-diff interval leg tripped its ±0.35 s tolerance under post-matrix load (video 6.183 s vs records 6.955 s, delta −0.772 s; both flips present on video exactly once each, per-leg timings in-family — a stamping/encoder jitter event, not a restyle discrepancy), green isolated ×1 (26.2 s) and in this clean run — the leg's third recorded flake (2026-08-30, 2026-09-01 precedents); tier 1 **277** + tier 2 **8/8** on the same tree; Ryzen 5 7500F, Arch, Anki 26.08.1, Omarchy 4.0 line | No theming/mapping/timing code touched since v0.1.1 (behavior-identical audit cuts + meta rename + preview builder script); all populations in family with the six prior green runs (sessions 1022–1054 ms, startup 5.6–18.9 ms) |
